const fs = require('fs');
let code = fs.readFileSync('components/Game.tsx', 'utf8');

code = code.replace("import Room2D from './Room2D';", "import EscapeRoom from './EscapeRoom';\nimport { Smartphone, Laptop, Book } from 'lucide-react';");

const oldInvestigation = `function Investigation(){const {state,dispatch}=useGame();const [filter,setFilter]=useState<'all'|Evidence['app']>('all'),[query,setQuery]=useState(''),[view,setView]=useState<'evidence'|'timeline'|'board'>('evidence'),[mobileTab,setMobileTab]=useState('read');
 const [activeObj,setActiveObj]=useState<string|null>(null);
 const visible=evidence.filter(e=>e.chapter<=state.unlocked),list=visible.filter(e=>(filter==='all'||e.app===filter)&&(e.title+' '+e.id+' '+e.author).toLocaleLowerCase('vi').includes(query.toLocaleLowerCase('vi')));
 const selected=evidence.find(e=>e.id===state.selectedEvidence)!;
 const choose=(id:string)=>{dispatch({type:'evidence',id});setView('evidence');setMobileTab('read');};
 return <><div className="chapter-strip"><div><span className="eyebrow">CHƯƠNG 0{state.chapter}</span><h1>{chapters[state.chapter-1].title}</h1></div><p>{chapters[state.chapter-1].subtitle}</p></div><Room2D onInteract={setActiveObj}/>{activeObj&&<div className="modal-overlay point-and-click" onClick={(e)=>{if(e.target===e.currentTarget)setActiveObj(null)}}><dialog open><div className="modal-head" style={{marginBottom:0,paddingBottom:0,borderBottom:'none'}}><button className="icon-button" onClick={()=>setActiveObj(null)} style={{marginLeft:'auto'}}>Đóng</button></div><div className={\`investigation mobile-\${mobileTab}\`} style={{flex:1,minHeight:0}}>{(activeObj==='computer'||activeObj==='all')&&<><nav className="app-dock" aria-label="Ứng dụng hồ sơ">{([['all',LayoutGrid,'Tất cả'],['chat',MessageSquare,'Tin nhắn'],['mail',Mail,'Hộp thư'],['terminal',Terminal,'Nhật ký'],['lab',FlaskConical,'Tái dựng']] as const).map(([id,Icon,label])=><button key={id} className={filter===id?'active':''} aria-label={label} onClick={()=>{setFilter(id);setMobileTab('files');setView('evidence');}}><Icon size={21}/><span>{label}</span>{id==='chat'&&<i/>}</button>)}<span className="dock-bottom"><LockKeyhole size={17}/><small>HS–01</small></span></nav><main className="reader-column" style={{flex:1}}><EvidenceReader item={selected}/></main><TaskPanel/></>}{(activeObj==='files')&&<aside className="evidence-library" style={{flex:1,borderRight:'none'}}><div className="library-head"><strong>Chứng cứ</strong><span>{visible.length}<small> / 17</small></span></div><label className="search-box"><Search size={15}/><input aria-label="Tìm chứng cứ" placeholder="Tìm trong hồ sơ…" value={query} onChange={e=>setQuery(e.target.value)}/></label><div className="evidence-list">{list.map(e=>{const Icon=icons[e.app];return <button className={\`evidence-item \${state.selectedEvidence===e.id&&view==='evidence'?'active':''}\`} key={e.id} onClick={()=>choose(e.id)}><span className="evidence-item-icon"><Icon size={17}/></span><span><small>{e.id} <span>{e.author}</span></small><strong>{e.title}</strong><em>{e.summary}</em></span><span className="item-state">{state.pinned.includes(e.id)?<Pin size={12}/>:!state.opened.includes(e.id)?<i/>:<Check size={12}/>}</span></button>;})}{!list.length&&<p className="empty-list">Không có chứng cứ phù hợp.</p>}</div><div className="library-foot"><LockKeyhole size={12}/> Chương sau mở thêm dữ kiện</div></aside>}{activeObj==='board'&&<main className="reader-column" style={{flex:1}}><section className="workspace-page"><span className="eyebrow">NHẬN ĐỊNH & CĂN CỨ</span><h2>Bảng lập luận / Dòng thời gian</h2><h3>Chứng cứ đã ghim</h3><div className="pinned-grid">{state.pinned.map(id=>{const e=evidence.find(e=>e.id===id)!;return <button key={id} onClick={()=>choose(id)}><span>{id}<Pin size={14}/></span><strong>{e.title}</strong><p>{e.summary}</p></button>;})}{!state.pinned.length&&<p className="muted">Mở chứng cứ rồi chọn “Ghim chứng cứ” để đặt lên bảng.</p>}</div><h3>Lịch sử lập luận</h3>{tasks.filter(t=>state.answers[t.id]).map(t=>{const score=evaluateChallenge(t.id,state.answers[t.id]).score;return <div className="claim-card" key={t.id}><button onClick={()=>dispatch({type:'task',id:t.id})}><span>{t.id} · {score===4?'Có căn cứ':'Cần bổ sung'}</span><strong>{t.title}</strong><ArrowRight size={17}/></button><p>{state.attempts[t.id]?.length??0} lần ghi nhận · lần đầu {state.attempts[t.id]?.[0].evaluation.score??0}/4 → hiện tại {score}/4</p>{state.notes[t.id]&&<blockquote>{state.notes[t.id]}</blockquote>}</div>;})}{!Object.keys(state.answers).length&&<p className="muted">Các lập luận xuất hiện ở đây sau khi được ghi nhận.</p>}</section></main>}</div></dialog></div>}</>;
}`;

const newInvestigation = `function Investigation(){const {state,dispatch}=useGame();const [filter,setFilter]=useState<'all'|Evidence['app']>('all'),[query,setQuery]=useState(''),[view,setView]=useState<'evidence'|'timeline'|'board'>('evidence'),[mobileTab,setMobileTab]=useState('read');
 const [activeObj,setActiveObj]=useState<string|null>(null);
 const visible=evidence.filter(e=>e.chapter<=state.unlocked),list=visible.filter(e=>(filter==='all'||e.app===filter)&&(e.title+' '+e.id+' '+e.author).toLocaleLowerCase('vi').includes(query.toLocaleLowerCase('vi')));
 const selected=evidence.find(e=>e.id===state.selectedEvidence)!;
 const choose=(id:string)=>{dispatch({type:'evidence',id});setView('evidence');setMobileTab('read');};
 
 const inventory = (
   <div className="inventory-bar">
     <button onClick={() => { setActiveObj('phone'); setFilter('chat'); }}><Smartphone size={24}/> Điện thoại</button>
     <button onClick={() => { setActiveObj('laptop'); setFilter('all'); }}><Laptop size={24}/> Laptop</button>
     <button onClick={() => setActiveObj('notebook')}><Book size={24}/> Sổ điều tra</button>
   </div>
 );

 return <><div className="chapter-strip"><div><span className="eyebrow">CHƯƠNG 0{state.chapter}</span><h1>{chapters[state.chapter-1].title}</h1></div><p>{chapters[state.chapter-1].subtitle}</p></div><EscapeRoom onInteract={setActiveObj}/>{inventory}{activeObj&&<div className="modal-overlay point-and-click" onClick={(e)=>{if(e.target===e.currentTarget)setActiveObj(null)}}><dialog open><div className="modal-head" style={{marginBottom:0,paddingBottom:0,borderBottom:'none'}}><button className="icon-button" onClick={()=>setActiveObj(null)} style={{marginLeft:'auto'}}>Đóng</button></div><div className={\`investigation mobile-\${mobileTab}\`} style={{flex:1,minHeight:0}}>{(activeObj==='phone')&&<><nav className="app-dock" aria-label="Điện thoại">{([['chat',MessageSquare,'Tin nhắn'],['mail',Mail,'Hộp thư']] as const).map(([id,Icon,label])=><button key={id} className={filter===id?'active':''} aria-label={label} onClick={()=>{setFilter(id);setMobileTab('files');setView('evidence');}}><Icon size={21}/><span>{label}</span>{id==='chat'&&<i/>}</button>)}</nav><main className="reader-column" style={{flex:1}}><EvidenceReader item={selected}/></main></>}{(activeObj==='laptop'||activeObj==='files')&&<><nav className="app-dock" aria-label="Laptop">{([['all',LayoutGrid,'Tất cả'],['terminal',Terminal,'Nhật ký'],['lab',FlaskConical,'Tái dựng']] as const).map(([id,Icon,label])=><button key={id} className={filter===id?'active':''} aria-label={label} onClick={()=>{setFilter(id);setMobileTab('files');setView('evidence');}}><Icon size={21}/><span>{label}</span>{id==='chat'&&<i/>}</button>)}</nav><aside className="evidence-library" style={{flex:1,borderRight:'none',maxWidth:'300px'}}><div className="library-head"><strong>Chứng cứ</strong><span>{visible.length}<small> / 17</small></span></div><label className="search-box"><Search size={15}/><input aria-label="Tìm chứng cứ" placeholder="Tìm trong hồ sơ…" value={query} onChange={e=>setQuery(e.target.value)}/></label><div className="evidence-list">{list.map(e=>{const Icon=icons[e.app];return <button className={\`evidence-item \${state.selectedEvidence===e.id&&view==='evidence'?'active':''}\`} key={e.id} onClick={()=>choose(e.id)}><span className="evidence-item-icon"><Icon size={17}/></span><span><small>{e.id} <span>{e.author}</span></small><strong>{e.title}</strong><em>{e.summary}</em></span><span className="item-state">{state.pinned.includes(e.id)?<Pin size={12}/>:!state.opened.includes(e.id)?<i/>:<Check size={12}/>}</span></button>;})}{!list.length&&<p className="empty-list">Không có chứng cứ phù hợp.</p>}</div></aside><main className="reader-column" style={{flex:2}}><EvidenceReader item={selected}/></main></>}{activeObj==='notebook'&&<><TaskPanel/></>}{activeObj==='board'&&<main className="reader-column" style={{flex:1}}><section className="workspace-page"><span className="eyebrow">NHẬN ĐỊNH & CĂN CỨ</span><h2>Bảng lập luận / Dòng thời gian</h2><h3>Chứng cứ đã ghim</h3><div className="pinned-grid">{state.pinned.map(id=>{const e=evidence.find(e=>e.id===id)!;return <button key={id} onClick={()=>choose(id)}><span>{id}<Pin size={14}/></span><strong>{e.title}</strong><p>{e.summary}</p></button>;})}{!state.pinned.length&&<p className="muted">Mở chứng cứ rồi chọn “Ghim chứng cứ” để đặt lên bảng.</p>}</div><h3>Lịch sử lập luận</h3>{tasks.filter(t=>state.answers[t.id]).map(t=>{const score=evaluateChallenge(t.id,state.answers[t.id]).score;return <div className="claim-card" key={t.id}><button onClick={()=>dispatch({type:'task',id:t.id})}><span>{t.id} · {score===4?'Có căn cứ':'Cần bổ sung'}</span><strong>{t.title}</strong><ArrowRight size={17}/></button><p>{state.attempts[t.id]?.length??0} lần ghi nhận · lần đầu {state.attempts[t.id]?.[0].evaluation.score??0}/4 → hiện tại {score}/4</p>{state.notes[t.id]&&<blockquote>{state.notes[t.id]}</blockquote>}</div>;})}{!Object.keys(state.answers).length&&<p className="muted">Các lập luận xuất hiện ở đây sau khi được ghi nhận.</p>}</section></main>}</div></dialog></div>}</>;
}`;

code = code.replace(oldInvestigation, newInvestigation);
fs.writeFileSync('components/Game.tsx', code);
